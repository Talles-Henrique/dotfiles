backend = "xrender";
vsync = true;
active-opacity = 1.0;
inactive-opacity = 1.0;
frame-opacity = 1.0;

# Esta regra força o Alacritty a aceitar a transparência do sistema
opacity-rule = [
  "90:class_g = 'Alacritty'"
];

corner-radius = 0;
